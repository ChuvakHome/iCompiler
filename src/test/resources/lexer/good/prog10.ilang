routine main() is
  for i in 2..100
  loop
    var n is 2
    var is_simple is true
    while n * n <= i
    loop
      is_simple := is_simple and (i % n /= 0)
      n := n + 1
    end
    if is_simple
    then
      print i
    end
  end
end
